name: Build Wi-Fi Visualizer

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build macOS app
    strategy:
      matrix:
        include:
          - arch: arm64
            runner: macos-14
          - arch: x86_64
            runner: macos-15-intel

    runs-on: ${{ matrix.runner }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build application
        run: |
          rm -rf build dist
          pyinstaller \
            --noconfirm \
            --clean \
            --windowed \
            --name "Wi-Fi Visualizer" \
            dashboard.py

      - name: Create administrator launcher
        run: |
          cat > launcher.applescript <<'APPLESCRIPT'
          set launcherPath to POSIX path of (path to me)
          set parentFolder to do shell script "dirname " & quoted form of launcherPath
          set appPath to parentFolder & "/Wi-Fi Visualizer.app/Contents/MacOS/Wi-Fi Visualizer"

          do shell script quoted form of appPath with administrator privileges
          APPLESCRIPT

          osacompile \
            -o "dist/Wi-Fi Visualizer Launcher.app" \
            launcher.applescript

      - name: Create distribution folder
        run: |
          mkdir -p "dist/Wi-Fi Visualizer"

          mv \
            "dist/Wi-Fi Visualizer.app" \
            "dist/Wi-Fi Visualizer/"

          mv \
            "dist/Wi-Fi Visualizer Launcher.app" \
            "dist/Wi-Fi Visualizer/"

      - name: Create ZIP
        run: |
          cd dist
          zip -r "../WiFi-Visualizer-macOS-${{ matrix.arch }}.zip" \
            "Wi-Fi Visualizer"

      - name: Upload build
        uses: actions/upload-artifact@v4
        with:
          name: WiFi-Visualizer-macOS-${{ matrix.arch }}
          path: WiFi-Visualizer-macOS-${{ matrix.arch }}.zip

  release:
    name: Create GitHub Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')

    runs-on: ubuntu-latest

    steps:
      - name: Download builds
        uses: actions/download-artifact@v4
        with:
          path: release-files
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: release-files/*.zip
